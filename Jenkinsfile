pipeline {
    agent any // This specifies that the pipeline can run on any available agent

    stages {
        stage('Build') {
            steps {
                // Commands to build your project, for example:
                echo "hello"
                // Assuming a Maven-based project
            }
        }
    }
}
